// Supabase client initialization wrapper for compatibility
// This file maintains the same export interface as the original SQLite schema.js
const supabase = require('./supabase');

// Export the supabase client to maintain compatibility with any code that imports this
module.exports = supabase;