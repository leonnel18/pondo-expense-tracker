// mask.js — Single source of truth for the privacy-mask redaction string (US-02, v1.2).
// Imported by every touched formatter so the redaction style is visually consistent everywhere.
export const MASK_PLACEHOLDER = '₱ ••••';